{# This is rendered in the users settings menu as a separate card #}

<div piwik-content-block content-title="{{ 'RebelOIDC_OpenIDConnect'|translate }}">

    <p>
        {{ 'RebelOIDC_OIDCIntro'|translate }}
    </p>

    {% if isLinked %}
        <p>
            <strong>{{ 'RebelOIDC_AccountLinked'|translate(remoteUserId) }}</strong>
            <br />
            <br />
            <form method="post" action="{{ linkTo({'module': 'RebelOIDC', 'action': 'unlink'}) }}">
                <input type="hidden" name="form_nonce" value="{{ nonce }}" />
                <button class="red lighten-2 btn" type="submit">
                    {{ 'RebelOIDC_Unlink'|translate }}
                </button>
            </form>
        </p>
        {% else %}
        <p>
            <strong>{{ 'RebelOIDC_AccountNotLinked'|translate }}</strong>
            <br />
            <br />
            <form method="post" action="{{ linkTo({'module': 'RebelOIDC', 'action': 'signin'}) }}">
                <input type="hidden" name="form_nonce" value="{{ nonce }}" />
                <button class="btn" type="submit">
                    {{ 'RebelOIDC_Link'|translate }}
                </button>
            </form>
        </p>
    {% endif %}

</div>
